# ============================================================================
# HumanGov expiry-monitor workflow settings
# ============================================================================

local-simulation:
  user-workflow:
    workflow-name: "humangov-expiry-monitor-local"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.local-simulation.json"
    secrets-path: "../../secrets.yaml"

staging-settings:
  user-workflow:
    workflow-name: "humangov-expiry-monitor-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "humangov-expiry-monitor-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../../secrets.yaml"
